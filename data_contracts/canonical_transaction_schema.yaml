event_ts:

  type: datetime

  nullable: false

  timezone: UTC

  source_column: Timestamp

  semantics:
    "Synthetic transaction event timestamp."

  timezone_policy:
    source_timezone_available: false
    interpretation: UTC
    assumption_documented: true